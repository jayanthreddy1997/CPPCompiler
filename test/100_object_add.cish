class Calculator {
  let x;
  let y;

  Calculator(this, a, b){
    this.x = a;
    this.y = b;
  }

  add(this){
    return this.x+this.y;
  }
}

main() {
  let Calculator obj = new Calculator(10, 5);
  let x = obj.add();
  return x;
}