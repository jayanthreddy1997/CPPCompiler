class A {
  let z;

  A(this) {}

  get(this){
    return this.z;
  }

  set(this, x){
    this.z = x;
  }
};

main() {
  let A obj = new A(); {
    obj.set(1);
    let x = obj.get();{
      return x;
    }
  }
}