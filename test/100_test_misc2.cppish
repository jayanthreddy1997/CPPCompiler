class Calculator {
  let x;
  let y;
  Calculator (this, x1, y1){
    this.x = x1;
    this.y = y1;
  }

  add(this){
    return this.x + this.y;
  }
};

main() {
  let Calculator obj = new Calculator(2, 3);
  return obj.add();
}