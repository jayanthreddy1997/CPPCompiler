class Calculator {
  let x;
  let y;
  Calculator (this, x1, y1){
    this.x = x1;
    this.y = y1;
  }

  add(this){
    return this.x + this.y;
  }
};

main() {
    let unique_ptr<Calculator> up = new Calculator(3, 4);
    let unique_ptr<Calculator> y = up;
    return y.add();
}